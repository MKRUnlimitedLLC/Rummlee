Rummlee Ship — commit these files and push origin/main, then force the production deploy and run migrations.

Do not redesign. Do not revert researcher pay. Do not change Stripe, checkout charges, referral math, or test mode. Charges stay off. Test credits stay the wallet. TEST_MODE stays true. Do not display lead or ambassador pay on any public page.

Repo: https://github.com/MKRUnlimitedLLC/Rummlee
Branch: main
Parent already on main: 768d133

Commit message:
Add house shelf and Rummlee Rep modules required by researcher pay.

Paths in this zip:
- migrations/0025_house.sql
- migrations/0026_rep.sql
- migrations/0027_facts.sql
- migrations/0028_research.sql
- migrations/0029_trio.sql
- migrations/0030_researcher_account.sql
- migrations/0031_reveal.sql
- src/lib/rummlee/house.ts
- src/lib/rummlee/rep.ts
- src/lib/rummlee/types.ts
- src/lib/rummlee/seed.ts
- src/routes/rep.tsx
- src/routeTree.gen.ts

Why:
768d133 imports ./house and ./rep and assigns plusTier, rep, sellerRep, alwaysOn, and disposition. Those modules and the Profile/Listing/Sale/Order fields were not on main. Migrations 0025–0031 create the tables and columns those files use. 0032–0037 are already on main. Do not replace them.

seed.ts only adds the ensureHouse() call so the Fargo shelf account exists. routeTree.gen.ts registers /rep. The other routes in that file are already on main.

After this lands, npm run typecheck must no longer report:
- Cannot find module './house' or './rep'
- Property 'plusTier' / 'rep' / 'sellerRep' / 'alwaysOn' / 'disposition' missing on Profile, Listing, Sale, or Order
